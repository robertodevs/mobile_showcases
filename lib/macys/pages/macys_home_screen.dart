import 'package:flutter/material.dart';

class MacysHomeScreen extends StatelessWidget {
  const MacysHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
